package br.com.abiblia.specification;
import org.springframework.data.jpa.domain.Specification;

import br.com.abiblia.entidades.Versiculo;

public class VersiculoSpecification {
 
     /*
    public static Specification<Versiculo> name(Long id) {
        return (root, criteriaQuery, criteriaBuilder) -> criteriaBuilder.equal(root.get(Versiculo_.id), id);
    }
 
    public static Specification<Person> phone(Integer phone) {
        return (root, criteriaQuery, criteriaBuilder) ->
                criteriaBuilder.equal(root.get(Person_.phone), phone);
    }
 
    public static Specification<Person> cpf(Integer cpf) {
        return (root, criteriaQuery, criteriaBuilder) ->
                criteriaBuilder.equal(root.get(Person_.cpf), cpf);
    }
 
 */
}